---
alwaysApply: true
---

# AI Crypto Advisor - Complete Implementation Plan

## Tech Stack

| Component | Technology |
|-----------|-----------|
| Frontend | React + TypeScript (Vite) |
| Backend | Python (FastAPI) |
| Database | PostgreSQL |
| ORM | SQLAlchemy |
| Auth | JWT (python-jose + bcrypt) |
| APIs | CoinGecko, CryptoPanic, OpenRouter |
| Memes | Static JSON |
| Version Control | Git + GitHub |

### Deployment

| Component | Platform |
|-----------|----------|
| Frontend | **Vercel** (free) |
| Backend | **Railway** (free tier) |
| Database | **Railway** PostgreSQL (free tier) |

---

## Project Structure

```
moveoGroup/
├── frontend/                # React + TypeScript (deploy to Vercel)
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── services/
│   │   └── App.tsx
│   ├── package.json
│   └── vite.config.ts
│
├── backend/                 # Python FastAPI (deploy to Railway)
│   ├── app/
│   │   ├── main.py
│   │   ├── routers/
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── services/
│   │   └── auth/
│   ├── requirements.txt
│   └── .env
│
├── README.md
└── plan.mdc
```

---

## Submission Requirements

When submitting your solution, attach the following:

1. [ ] **Project Description** - An overall description of the assignment in English (no more than 1 page)
2. [ ] **GitHub Repository** - Upload code to GitHub and attach link (public repo)
3. [ ] **Deployed App URL** - Deploy the project and supply the URL
4. [ ] **Send to:** hr-team@moveo.group

---

## Git Workflow

### Initial Setup (Phase 1)
```bash
git init
git remote add origin https://github.com/YOUR_USERNAME/crypto-advisor.git
git add .
git commit -m "Initial commit: Project setup"
git push -u origin main
```

### Commit After Each Phase
| After Phase | Commit Message |
|-------------|---------------|
| Phase 1 | `feat: project setup with backend and frontend structure` |
| Phase 2 | `feat: authentication with signup and login` |
| Phase 3 | `feat: onboarding quiz with user preferences` |
| Phase 4 | `feat: external APIs integration` |
| Phase 5 | `feat: dashboard UI with all sections` |
| Phase 6 | `feat: voting system` |
| Phase 7 | `feat: polish, deploy, and documentation` |

---

## Phase 1: Project Setup

### Backend (Python FastAPI)
- [x] **1.1** Create `backend/` folder
- [x] **1.2** Create virtual environment: `python -m venv venv`
- [x] **1.3** Create `requirements.txt` with dependencies
- [x] **1.4** Set up FastAPI app structure (main.py, routers, models)
- [x] **1.5** Configure PostgreSQL connection with SQLAlchemy
- [x] **1.6** Create database models: User, Preferences, Vote
- [x] **1.7** Run database migrations

### Frontend (React + Vite)
- [x] **1.8** Create React app with Vite: `npm create vite@latest frontend`
- [x] **1.9** Install dependencies: axios, react-router-dom, etc.
- [x] **1.10** Create folder structure: components, pages, services
- [x] **1.11** Configure environment variables for API URL

### Git
- [x] **1.12** Create `.gitignore` for Python and Node
- [x] **1.13** Git: Initialize repo, first commit and push

---

## Phase 2: Authentication

### Backend
- [x] **2.1** Create auth utilities: JWT token creation/validation
- [x] **2.2** Create signup endpoint: POST /api/auth/signup
- [x] **2.3** Create login endpoint: POST /api/auth/login
- [x] **2.4** Create get current user endpoint: GET /api/auth/me
- [x] **2.5** Add JWT middleware to protect routes

### Frontend
- [x] **2.6** Create AuthContext for managing login state
- [x] **2.7** Build signup page: form with email, name, password
- [x] **2.8** Build login page: form with email and password
- [x] **2.9** Add form validation with error messages
- [x] **2.10** Store JWT token in localStorage
- [x] **2.11** Add protected route component

### Git
- [x] **2.12** Git: Commit and push authentication feature

---

## Phase 3: Onboarding Quiz

### Backend
- [x] **3.1** Create preferences endpoint: POST /api/user/preferences
- [x] **3.2** Create get preferences endpoint: GET /api/user/preferences
- [x] **3.3** Update user onboardingComplete flag after saving

### Frontend
- [x] **3.4** Create onboarding page with multi-step wizard layout
- [x] **3.5** Build Step 1: Crypto assets selection (checkboxes)
- [x] **3.6** Build Step 2: Investor type selection (radio buttons)
- [x] **3.7** Build Step 3: Content type preferences (checkboxes)
- [x] **3.8** Connect form to backend API
- [x] **3.9** Redirect to dashboard after completion
- [x] **3.10** Handle returning users: skip if already completed

### Git
- [x] **3.11** Git: Commit and push onboarding feature

---

## Phase 4: External APIs Integration

### Backend Services
- [x] **4.1** Create CoinGecko service: fetch coin prices
- [x] **4.2** Create CryptoPanic service: fetch news with static fallback
- [x] **4.3** Create OpenRouter service: generate AI insight
- [x] **4.4** Create static memes JSON file

### Backend API Routes
- [x] **4.5** Create GET /api/content/prices - coin prices
- [x] **4.6** Create GET /api/content/news - market news
- [x] **4.7** Create GET /api/content/insight - AI insight
- [x] **4.8** Create GET /api/content/meme - random meme
- [x] **4.9** Add error handling and fallbacks

### Git
- [x] **4.10** Git: Commit and push API integrations

---

## Phase 5: Dashboard UI

### Frontend Components
- [x] **5.1** Create dashboard page layout with 4-section grid
- [x] **5.2** Build Coin Prices section: display coins with price, change %
- [x] **5.3** Build Market News section: display news headlines with links
- [x] **5.4** Build AI Insight section: display AI-generated insight card
- [x] **5.5** Build Meme section: display random crypto meme

### Frontend Features
- [x] **5.6** Create API service to fetch content from backend
- [x] **5.7** Add loading states with skeleton loaders
- [x] **5.8** Add error states with friendly messages
- [x] **5.9** Personalize content based on user preferences

### Git
- [x] **5.10** Git: Commit and push dashboard UI

---

## Phase 6: Voting System

### Backend
- [ ] **6.1** Create vote endpoint: POST /api/vote
- [ ] **6.2** Create get user votes endpoint: GET /api/votes

### Frontend
- [ ] **6.3** Build thumbs up/down vote component
- [ ] **6.4** Add voting buttons to all 4 dashboard sections
- [ ] **6.5** Connect voting to backend API
- [ ] **6.6** Add visual feedback showing selected state

### Git
- [ ] **6.7** Git: Commit and push voting system

---

## Phase 7: Polish and Deploy

### UI/UX
- [ ] **7.1** UI improvements: clean design, consistent styling
- [ ] **7.2** Responsive design: mobile-friendly layout

### Deployment - Backend + DB (Railway)
- [ ] **7.3** Create Railway project
- [ ] **7.4** Deploy PostgreSQL database on Railway
- [ ] **7.5** Deploy backend (Python) on Railway
- [ ] **7.6** Configure backend environment variables in Railway
- [ ] **7.7** Get backend URL (e.g., https://your-backend.up.railway.app)

### Deployment - Frontend (Vercel)
- [ ] **7.8** Create Vercel project (connect GitHub repo)
- [ ] **7.9** Configure frontend to use Railway backend URL
- [ ] **7.10** Deploy frontend to Vercel
- [ ] **7.11** Get frontend URL (e.g., https://your-app.vercel.app)
- [ ] **7.12** Test deployed app: verify all features work

### Documentation
- [ ] **7.13** Write README.md: project description, setup instructions
- [ ] **7.14** Write AI usage summary: document Cursor/AI interactions
- [ ] **7.15** (Bonus) Write training process suggestions document

### Git
- [ ] **7.16** Git: Final commit, push, verify repo is public

---

## Deliverables Checklist

- [ ] GitHub repository (public) - link to share
- [ ] Deployed frontend URL - Vercel URL to share
- [ ] Deployed backend URL - Railway URL (for reference)
- [ ] Database access credentials - provide to Moveo
- [ ] AI usage summary document - document Cursor/AI interactions
- [ ] Project description (1 page max) - overview of the assignment
- [ ] (Bonus) Training process suggestions

---

## Database Schema

```python
# SQLAlchemy Models

class User(Base):
    __tablename__ = "users"
    
    id = Column(UUID, primary_key=True, default=uuid4)
    email = Column(String, unique=True, nullable=False)
    name = Column(String, nullable=False)
    password_hash = Column(String, nullable=False)
    onboarding_complete = Column(Boolean, default=False)
    created_at = Column(DateTime, default=datetime.utcnow)
    
    preferences = relationship("Preferences", back_populates="user")
    votes = relationship("Vote", back_populates="user")


class Preferences(Base):
    __tablename__ = "preferences"
    
    id = Column(UUID, primary_key=True, default=uuid4)
    user_id = Column(UUID, ForeignKey("users.id"), unique=True)
    crypto_assets = Column(ARRAY(String))  # ["BTC", "ETH", "SOL"]
    investor_type = Column(String)          # "hodler" | "daytrader" | "nft"
    content_types = Column(ARRAY(String))   # ["news", "charts", "social", "fun"]
    
    user = relationship("User", back_populates="preferences")


class Vote(Base):
    __tablename__ = "votes"
    
    id = Column(UUID, primary_key=True, default=uuid4)
    user_id = Column(UUID, ForeignKey("users.id"))
    content_type = Column(String)   # "news" | "price" | "ai" | "meme"
    content_id = Column(String)
    is_upvote = Column(Boolean)
    created_at = Column(DateTime, default=datetime.utcnow)
    
    user = relationship("User", back_populates="votes")
```

---

## API Endpoints Summary

### Auth
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | /api/auth/signup | Register new user |
| POST | /api/auth/login | Login, returns JWT |
| GET | /api/auth/me | Get current user info |

### User
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | /api/user/preferences | Save user preferences |
| GET | /api/user/preferences | Get user preferences |

### Content
| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | /api/content/prices | Get coin prices |
| GET | /api/content/news | Get market news |
| GET | /api/content/insight | Get AI insight |
| GET | /api/content/meme | Get random meme |

### Voting
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | /api/vote | Submit vote |
| GET | /api/votes | Get user's votes |

---

## API Keys Required

| API | Free? | Register At |
|-----|-------|-------------|
| CoinGecko | Yes (no key needed) | - |
| CryptoPanic | Yes (free tier) | https://cryptopanic.com/developers/api/ |
| OpenRouter | Yes (free tier) | https://openrouter.ai/ |

---

## Backend Dependencies (requirements.txt)

```
fastapi
uvicorn
sqlalchemy
psycopg2-binary
python-jose[cryptography]
passlib[bcrypt]
python-dotenv
httpx
pydantic
alembic
```

---

## Frontend Dependencies

```
react
react-dom
react-router-dom
axios
typescript
@types/react
```

---

## Estimated Time

| Phase | Estimated Time |
|-------|---------------|
| Phase 1: Setup | 2-3 hours |
| Phase 2: Auth | 3-4 hours |
| Phase 3: Onboarding | 2-3 hours |
| Phase 4: APIs | 2-3 hours |
| Phase 5: Dashboard | 3-4 hours |
| Phase 6: Voting | 1-2 hours |
| Phase 7: Deploy | 2-3 hours |
| **Total** | **15-22 hours** |
